# Run docker image

`$ docker run -it kanwal7/restraunt_keeper:latest`

# Run locally

`$ g++ main.cpp`
`$ ./a.out log-file.csv`

# Build docker image

`$ docker build -t kanwal7/restraunt_keeper:latest`
